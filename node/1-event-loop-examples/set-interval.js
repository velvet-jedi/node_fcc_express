setInterval(()=>{
    console.log('hello world')
}, 2000)        // every interval the callback function gets executed so we never exit
console.log('I will run first')

/* synchronous functions in JavaScript allow you to perform tasks without blocking
 * the execution of the rest of your code. Common asynchronous
 *  operations include network requests, file I/O, and timers.
 */