// local (i cant share this with other code)
const secret = 'SUPER SECRET'

// share
const john = 'john'
const ranjana = 'ranjana'

// console.log(module)
// lets share what we can as an object because we have multiple values to share
module.exports = { john, ranjana }